{
  "name": "ton-connector",
  "prettier": "redstone-prettier-config",
  "version": "0.0.1",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc -build",
    "contract": "npx func-js func/stdlib.fc func/counter.fc --boc func/counter.cell && yarn build && ts-node src/start.ts --deploy=1",
    "start": "node dist/src/ton.js",
    "ton": "yarn build && ts-node src/start.ts"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "redstone-sdk": "^1.0.9",
    "ton": "^13.5.0",
    "ton-crypto": "^3.2.0",
    "ton-core": "^0.49.1",
    "@orbs-network/ton-access": "^2.3.3"
  },
  "devDependencies": {
    "@types/node": "^18.13.0",
    "typescript": "^4.9.5",
    "@ton-community/func-js": "^0.6.1"
  },
  "references": [
    {
      "path": "../protocol"
    },
    {
      "path": "../sdk"
    }
  ]
}
